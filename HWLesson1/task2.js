console.log('Task2');
const total = 100;
let ordered = 50;

console.log(`ordere = ${ordered}`);
console.log(
  total >= ordered
    ? 'Заказ оформлен, с вами свяжется менеджер'
    : 'На складе недостаточно твоаров!',
);

ordered = 20;
console.log(`ordere = ${ordered}`);
console.log(
  total >= ordered
    ? 'Заказ оформлен, с вами свяжется менеджер'
    : 'На складе недостаточно твоаров!',
);

ordered = 80;
console.log(`ordere = ${ordered}`);
console.log(
  total >= ordered
    ? 'Заказ оформлен, с вами свяжется менеджер'
    : 'На складе недостаточно твоаров!',
);

ordered = 130;
console.log(`ordere = ${ordered}`);
console.log(
  total >= ordered
    ? 'Заказ оформлен, с вами свяжется менеджер'
    : 'На складе недостаточно твоаров!',
);
